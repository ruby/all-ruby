--- parse.y-	2014-09-14 15:32:52.519774881 +0900
+++ parse.y	2014-09-14 15:32:54.715766460 +0900
@@ -611,7 +611,7 @@
 			value_expr($3);
 			$$ = node_assign($1, $3);
 		    }
-		| variable tOP_ASGN {$$ = assignable($1, 0);} arg
+		| variable tOP_ASGN {$<node>$ = assignable($1, 0);} arg
 		    {
 			if ($2 == tOROP) {
 			    $<node>3->nd_value = $4;
